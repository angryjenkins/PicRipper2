<div class="container">
  <h1>{{data.origin}}</h1>

  <div class="carousel">
    {{#each data}}
      <a class="carousel-item" href="#one!"><img src="{{this.url}}" alt="{{this.origin}} - {{this.id}}"></a>
    {{/each}}
  </div>
</div>